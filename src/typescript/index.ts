console.log("Algorithm Visualizer is running!");

// Example: simple array sorting visualizer logic
const arr: number[] = [5, 2, 9, 1, 5, 6];
arr.sort((a, b) => a - b);
console.log("Sorted array:", arr);
